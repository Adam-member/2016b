/*package org.usfirst.frc.team2557.robot.subsystems;

import edu.wpi.first.wpilibj.command.Subsystem;
import java.lang.Math;
/**
 *
 */
/*public class Boulder extends Subsystem {
	private double x;
    private double y;
    private boolean boulder;
    // Put methods for controlling this subsystem
    // here. Call these from Commands.

    public void initDefaultCommand() {
        // Set the default command for a subsystem here.
        //setDefaultCommand(new MySpecialCommand());
    }
    public void findBoulder(){
    	x = lowPointInches;
    	y = startInches;
    	if(lowPointInches - startInches > 4.5 || endPointInches - lowPointInches > 4.5 || lowPointInches - startInches < 5.5 || endPointInches - lowPointInches < 5.5){
    		boulder = true;
    	}
    public void find Bouder(){
    	if(width > 4.5 || width < 5.5){
    		boulder = true;
    	}
    }
    }
}
*/
